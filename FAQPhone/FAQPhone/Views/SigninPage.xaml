<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:infr="clr-namespace:FAQPhone.Inferstructure;assembly=FAQPhone"
             x:Class="FAQPhone.Views.SigninPage"
             Title="{infr:Translate signin}">
    <ScrollView>
        <StackLayout Style="{StaticResource pageStyle}">
            <Entry x:Name="usernameEntry" Placeholder="{infr:Translate username}" Text="{Binding username, Mode=TwoWay}" />
            <Entry x:Name="passwordEntry" Placeholder="{infr:Translate password}" IsPassword="true" Text="{Binding password, Mode=TwoWay}" />
            <Button Text="{infr:Translate signin}" Command="{Binding SigninCommand}" StyleClass="Info" />
            <Button Text="{infr:Translate signup}" Command="{Binding SignupCommand}" StyleClass="Success" />
            <Button Text="{infr:Translate forgetpassword}" Command="{Binding ForgetPasswordCommand}" StyleClass="Link" />
            <Label x:Name="messageLabel" />
        </StackLayout>
    </ScrollView>
</ContentPage>