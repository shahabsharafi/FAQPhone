<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:infr="clr-namespace:FAQPhone.Inferstructure;assembly=FAQPhone"
             x:Class="FAQPhone.Views.SecurityCodePage"
             Title="{infr:Translate securitycode}">
    <ScrollView>
        <StackLayout Style="{StaticResource pageStyle}">
            <Entry x:Name="securitycodeEntry" Placeholder="{infr:Translate securitycode}" Text="{Binding securitycode, Mode=TwoWay}" />
            <Button Text="{infr:Translate checkcode}" Command="{Binding CheckCodeCommand}" StyleClass="Info" />
            <Label x:Name="messageLabel" Text="{Binding message}" />
        </StackLayout>
    </ScrollView>
</ContentPage>