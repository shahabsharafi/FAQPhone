<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:infr="clr-namespace:FAQPhone.Inferstructure;assembly=FAQPhone"
             x:Class="FAQPhone.Views.ResetPasswordPage"
             Title="{infr:Translate resetpassword}">
    <ScrollView>
        <StackLayout Style="{StaticResource pageStyle}">
            <Label Text="{Binding CodeResult.username}"/>
            <Entry x:Name="passwordEntry" Placeholder="{infr:Translate password}" IsPassword="true" Text="{Binding password, Mode=TwoWay}" />
            <Button Text="{infr:Translate resetpassword}" Command="{Binding ResetPasswordCommand}" />
            <Label Text="{Binding CountDisplay}"/>
        </StackLayout>
    </ScrollView>
</ContentPage>