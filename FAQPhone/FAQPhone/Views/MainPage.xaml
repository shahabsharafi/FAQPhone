<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:infr="clr-namespace:FAQPhone.Inferstructure;assembly=FAQPhone"
             x:Class="FAQPhone.Views.MainPage"
             Title="{infr:Translate mainpage}">
    <ScrollView>
        <StackLayout Style="{StaticResource pageStyle}">
            <Button Text="{infr:Translate create_faq}" Command="{Binding CreateFAQCommand}" />
            <Button Text="{infr:Translate receive_faq}" Command="{Binding ReceiveFAQCommand}" />
            <Button Text="{infr:Translate inprogress_faq}" Command="{Binding ShowInprogressCommand}" />
            <Button Text="{infr:Translate archived_faq}" Command="{Binding ShowArchivedCommand}" />
            <Button Text="{infr:Translate signout}" Command="{Binding SignoutCommand}" />
        </StackLayout>
    </ScrollView>
</ContentPage>