<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:infr="clr-namespace:FAQPhone.Infarstructure;assembly=FAQPhone"
             x:Class="FAQPhone.Views.AboutPage"
             Title="{infr:Translate about_us}">
    <ScrollView>
        <StackLayout Style="{StaticResource pageStyle}">
            <Button Text="Picture" Command="{Binding SendCommand}" StyleClass="Info" />
            <Label Text="{infr:Translate app_name}" FontSize="Large" HorizontalOptions="Center" />
            <Label Text="{infr:Translate app_version}" FontSize="Small" HorizontalOptions="Center" />
            <Label x:Name="b" />
        </StackLayout>
    </ScrollView>
</ContentPage>