<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:infr="clr-namespace:FAQPhone.Inferstructure;assembly=FAQPhone"
             x:Class="FAQPhone.Views.SendActivationPage"
             Title="{infr:Translate sendActivation}">
    <ScrollView>
        <StackLayout Style="{StaticResource pageStyle}">
            <Entry x:Name="mobileEntry" Placeholder="{infr:Translate mobile}" IsPassword="true" Text="{Binding mobile, Mode=TwoWay}" />
            <Button Text="{infr:Translate sendActivation}" Command="{Binding SendActivationCommand}" />
            <Label x:Name="messageLabel" />
        </StackLayout>
    </ScrollView>
</ContentPage>